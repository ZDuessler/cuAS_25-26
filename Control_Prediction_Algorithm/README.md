This folder contains the standalone proof-of-concept of the Kalman filter. It is useful only a demonstration and the actual filter is now in [`C3Mission_1.py` under `agent_core`](/agent_core/C3Mission_1.py).

To install dependencies, use a virtual environment then do:  
`pip install -r requirements.txt`